export const STORAGE_KEYS = {
  ACCESS_TOKEN: 'accessToken',
  REFRESH_TOKEN: 'refreshToken',
  EXPIRES_IN: 'expiresIn',
};

export enum ROLES {
  ADMIN = 'admin',
  USER = 'user',
  GUEST = 'guest',
}
